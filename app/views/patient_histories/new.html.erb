<div class="container mt-4">
  <h1>Nuevo Control para <%= @patient.first_name %> <%= @patient.last_name %></h1>

  <div class="card mt-4">
    <div class="card-body">
      <%= render 'form' %>
    </div>
  </div>
</div>
