<div class="container mt-4">
  <h1>Editar Control de <%= @patient.first_name %> <%= @patient.last_name %></h1>
  <p class="text-muted">Fecha: <%= @patient_history.visit_date.strftime("%d/%m/%Y") %></p>

  <div class="card mt-4">
    <div class="card-body">
      <%= render 'form' %>
    </div>
  </div>
</div>
