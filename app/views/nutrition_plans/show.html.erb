<div class="nutrition-plan-show-layout">
  <!-- Left Sidebar -->
  <aside class="left-sidebar">
    <nav class="sidebar-nav">
      <%= link_to nutritionist_dashboard_path, class: "nav-item" do %>
        <span class="nav-icon">📊</span>
        <span>Dashboard</span>
      <% end %>
      <%= link_to patients_path, class: "nav-item" do %>
        <span class="nav-icon">👥</span>
        <span>Pacientes</span>
      <% end %>
      <a href="#" class="nav-item active">
        <span class="nav-icon">🍎</span>
        <span>Planes Nutricionales</span>
      </a>
      <a href="#" class="nav-item">
        <span class="nav-icon">🤖</span>
        <span>Copiloto IA</span>
      </a>
      <a href="#" class="nav-item">
        <span class="nav-icon">⚙️</span>
        <span>Configuraciones</span>
      </a>
    </nav>

    <div class="sidebar-footer">
      <%= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier",
                    class: "user-avatar",
                    alt: "Nutritionist" %>
      <div class="user-info">
        <div class="user-name"><%= current_nutritionist&.first_name || "Nutritionist" %> <%= current_nutritionist&.last_name || "" %></div>
        <div class="user-role">Nutritionist</div>
      </div>
    </div>
  </aside>

  <!-- Main Content -->
  <main class="main-content-show">
    <!-- Header -->
    <div class="plan-show-header">
      <div class="header-left">
        <%= link_to patient_nutrition_plans_path(@patient), class: "back-button-show" do %>
          ←
        <% end %>
        <div class="header-info">
          <h1 class="plan-main-title"><%= @nutrition_plan.objective || "Plan Pérdida de Peso Verano" %></h1>
          <p class="plan-main-subtitle">para <%= @patient.first_name %> <%= @patient.last_name %></p>
        </div>
      </div>
      <div class="header-actions-show">
        <%= link_to edit_patient_nutrition_plan_path(@patient, @nutrition_plan), class: "btn-header-action" do %>
          ✏️ Editar Plan
        <% end %>
        <%= link_to "#", class: "btn-header-action" do %>
          📋 Duplicar Plan
        <% end %>
        <%= link_to "#", class: "btn-header-share" do %>
          🔗 Compartir
        <% end %>
      </div>
    </div>

    <!-- Plan Overview Section -->
    <div class="plan-overview-unified-card">
      <div class="overview-top-row">
        <div class="overview-unified-item">
          <div class="overview-unified-label">Objetivo</div>
          <div class="overview-unified-value"><%= @nutrition_plan.objective || "Control de peso, mejora metabólica y recuperación neurológica" %></div>
        </div>
        <div class="overview-unified-item">
          <div class="overview-unified-label">Vigente</div>
          <div class="overview-unified-value">
            <%= @nutrition_plan.start_date&.strftime("%d %b %Y") || "06 Oct 2025" %> - <%= @nutrition_plan.end_date&.strftime("%d %b %Y") || "06 Oct 2025" %>
          </div>
        </div>
        <div class="overview-unified-item">
          <div class="overview-unified-label">Estado</div>
          <div class="plan-status-badge-show">
            <%= (@nutrition_plan.status || 'Active').capitalize %>
          </div>
        </div>
      </div>

      <div class="overview-bottom-row">
        <div class="overview-unified-item">
          <div class="overview-unified-label">Calorías diarias</div>
          <div class="overview-unified-value"><%= @nutrition_plan.calories || 2200 %>.0 kcal</div>
        </div>
        <div class="overview-unified-item">
          <div class="overview-unified-label">Macros</div>
          <div class="overview-unified-value">P: <%= @nutrition_plan.protein || 110 %>.0g / G: <%= @nutrition_plan.fat || 70 %>.0g / C: <%= @nutrition_plan.carbs || 280 %>.0g</div>
        </div>
      </div>
    </div>

    <!-- Meal Distribution Section -->
    <div class="meal-distribution-section">
      <h2 class="section-title-show">Distribución de Comidas</h2>

      <% if @nutrition_plan.meal_distribution.present? %>
        <% first_day = @nutrition_plan.meal_distribution %>
        <% if first_day.is_a?(Hash) %>
          <% meal_configs = {
            'desayuno' => { title: 'Desayuno', calories: '~450 kcal' },
            'almuerzo' => { title: 'Almuerzo', calories: '~700 kcal' },
            'cena' => { title: 'Cena', calories: '~650 kcal' },
            'snacks' => { title: 'Snacks', calories: '~400 kcal' }
          } %>

          <% first_day.each do |meal_type, meal_data| %>
            <% next unless meal_configs.key?(meal_type) %>
            <% config = meal_configs[meal_type] %>
            <div class="meal-card-show">
              <h3 class="meal-card-title"><%= config[:title] %> (<%= config[:calories] %>)</h3>
              <p class="meal-description-text">
                <%= meal_data.is_a?(Hash) ? (meal_data['recipe'] || meal_data['ingredients'] || 'Ver detalles del plan') : meal_data %>
              </p>
            </div>
          <% end %>
        <% end %>
      <% else %>
        <div class="meal-card-show">
          <h3 class="meal-card-title">Desayuno (~450 kcal)</h3>
          <p class="meal-description-text">Yogur griego con frutas rojas, un puñado de granola y una cucharada de semillas de chía</p>
        </div>
        <div class="meal-card-show">
          <h3 class="meal-card-title">Almuerzo (~700 kcal)</h3>
          <p class="meal-description-text">Pechuga de pollo a la plancha con quinoa y una ensalada grande de espinacas, tomate cherry y pesto.</p>
        </div>
        <div class="meal-card-show">
          <h3 class="meal-card-title">Cena (~650 kcal)</h3>
          <p class="meal-description-text">Salmón al horno con espárragos y una porción de batata asada.</p>
        </div>
        <div class="meal-card-show">
          <h3 class="meal-card-title">Snacks (~400 kcal)</h3>
          <p class="meal-description-text">Un puñado de almendras y un batido de proteínas post- entrenamiento.</p>
        </div>
      <% end %>
    </div>

    <!-- Nutritionist Notes Section -->
    <div class="notes-section">
      <h2 class="section-title-show">Notas del Nutricionista</h2>
      <div class="notes-content-show">
        <p><%= simple_format(@nutrition_plan.notes || "Hola Ana, hemos diseñado este plan para ayudarte a alcanzar tu objetivo de pérdida de peso de forma sostenible. Recuerda mantener bien hidratada y hacer todo tu plan, bebiendo agua en cada comida. No dudes en intercambiar el pollo por pavo o pescado blanco para variar.

Es importante que realices los entrenamientos de fuerza 3 veces por semana 2 días de cardio. Esto ayudará para mantener la masa muscular. ¡Mucho ánimo!") %></p>
      </div>
    </div>

    <!-- Key Tips Section -->
    <div class="tips-section">
      <h2 class="section-title-show">Consejos Clave</h2>
      <div class="tips-list">
        <div class="tip-card">
          <div class="tip-icon-check">✓</div>
          <div class="tip-content-show">
            <h4 class="tip-title-show">Hidratación constante</h4>
            <p class="tip-description-show">Bebe al menos 8 vasos de agua al día</p>
          </div>
        </div>
        <div class="tip-card">
          <div class="tip-icon-check">✓</div>
          <div class="tip-content-show">
            <h4 class="tip-title-show">Prioriza la Proteína</h4>
            <p class="tip-description-show">Asegúrate de incluir una fuente de proteína en cada comida principal.</p>
          </div>
        </div>
        <div class="tip-card">
          <div class="tip-icon-check">✓</div>
          <div class="tip-content-show">
            <h4 class="tip-title-show">Escucha a tu Cuerpo</h4>
            <p class="tip-description-show">Come cuando tengas hambre y detente cuando estés satisfecho.</p>
          </div>
        </div>
      </div>
    </div>
  </main>
</div>

<style>
/* Layout */
.nutrition-plan-show-layout {
  display: flex;
  min-height: 100vh;
  background-color: #f8fafc;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
}

/* Left Sidebar */
.left-sidebar {
  width: 260px;
  background: white;
  border-right: 1px solid #e5e7eb;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  padding: 24px 0;
  position: sticky;
  top: 0;
  height: 100vh;
}

.sidebar-nav {
  padding: 24px 24px 0 24px;
  flex: 1;
}

.nav-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px;
  margin-bottom: 4px;
  color: #6b7280;
  text-decoration: none;
  border-radius: 8px;
  transition: all 0.15s ease;
  font-weight: 500;
}

.nav-item:hover {
  background: #f3f4f6;
  color: #374151;
}

.nav-item.active {
  background: #ede9fe;
  color: #7c3aed;
}

.nav-icon {
  font-size: 18px;
  width: 20px;
  text-align: center;
}

.sidebar-footer {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 24px;
  border-top: 1px solid #f3f4f6;
}

.user-avatar {
  width: 40px;
  height: 40px;
  border-radius: 20px;
}

.user-name {
  font-weight: 600;
  color: #1f2937;
  font-size: 14px;
}

.user-role {
  font-size: 13px;
  color: #6b7280;
}

/* Main Content */
.main-content-show {
  flex: 1;
  padding: 32px 48px;
  max-width: 900px;
  margin: 0 auto;
}

/* Header */
.plan-show-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 32px;
  padding-bottom: 24px;
  border-bottom: 1px solid #e5e7eb;
}

.header-left {
  display: flex;
  align-items: center;
  gap: 16px;
}

.back-button-show {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  background: white;
  border: 1px solid #e5e7eb;
  border-radius: 8px;
  color: #6b7280;
  text-decoration: none;
  transition: all 0.15s ease;
  font-size: 20px;
}

.back-button-show:hover {
  background: #f9fafb;
  border-color: #d1d5db;
  color: #374151;
}

.plan-main-title {
  font-size: 24px;
  font-weight: 700;
  color: #1f2937;
  margin: 0 0 4px 0;
  line-height: 1.2;
}

.plan-main-subtitle {
  color: #6b7280;
  margin: 0;
  font-size: 14px;
}

.header-actions-show {
  display: flex;
  gap: 8px;
}

.btn-header-action,
.btn-header-share {
  padding: 8px 14px;
  border-radius: 8px;
  font-size: 13px;
  font-weight: 500;
  text-decoration: none;
  transition: all 0.2s ease;
  white-space: nowrap;
  border: 1px solid #e5e7eb;
  background: white;
  color: #374151;
}

.btn-header-action:hover {
  background: #f9fafb;
  border-color: #d1d5db;
}

.btn-header-share {
  color: #10b981;
  border-color: #10b981;
}

.btn-header-share:hover {
  background: #ecfdf5;
  border-color: #059669;
}

/* Plan Overview Unified Card */
.plan-overview-unified-card {
  background: white;
  border: 1px solid #e5e7eb;
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 24px;
}

.overview-top-row {
  display: grid;
  grid-template-columns: 2fr 1.5fr 1fr;
  gap: 32px;
  padding-bottom: 24px;
  border-bottom: 1px solid #f3f4f6;
  margin-bottom: 24px;
}

.overview-bottom-row {
  display: grid;
  grid-template-columns: 1fr 2fr;
  gap: 32px;
}

.overview-unified-item {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.overview-unified-label {
  font-size: 14px;
  color: #6b7280;
  font-weight: 500;
}

.overview-unified-value {
  font-size: 15px;
  color: #1f2937;
  font-weight: 600;
  line-height: 1.5;
}

.plan-status-badge-show {
  display: inline-block;
  padding: 6px 12px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 500;
  background: #dcfce7;
  color: #166534;
  width: fit-content;
}

/* Section Titles */
.section-title-show {
  font-size: 18px;
  font-weight: 700;
  color: #1f2937;
  margin: 0 0 20px 0;
}

/* Meal Distribution Section */
.meal-distribution-section {
  background: white;
  border: 1px solid #e5e7eb;
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 24px;
}

.meal-card-show {
  background: #f8fafc;
  border: 1px solid #f1f5f9;
  border-radius: 8px;
  padding: 20px;
  margin-bottom: 12px;
  transition: all 0.15s ease;
}

.meal-card-show:hover {
  background: #f3f4f6;
  border-color: #e5e7eb;
}

.meal-card-show:last-child {
  margin-bottom: 0;
}

.meal-card-title {
  font-size: 15px;
  font-weight: 600;
  color: #1f2937;
  margin: 0 0 8px 0;
}

.meal-description-text {
  font-size: 14px;
  line-height: 1.6;
  margin: 0;
  color: #6b7280;
}

/* Nutritionist Notes */
.notes-section {
  background: white;
  border: 1px solid #e5e7eb;
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 24px;
}

.notes-content-show {
  background: #fffbeb;
  border: 1px solid #fde68a;
  border-radius: 8px;
  padding: 20px;
}

.notes-content-show p {
  margin: 0;
  color: #78350f;
  line-height: 1.6;
  font-size: 14px;
}

/* Key Tips Section */
.tips-section {
  background: white;
  border: 1px solid #e5e7eb;
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 24px;
}

.tips-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.tip-card {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  padding: 16px;
  background: #ecfdf5;
  border: 1px solid #d1fae5;
  border-radius: 8px;
  transition: all 0.15s ease;
}

.tip-card:hover {
  background: #d1fae5;
  border-color: #a7f3d0;
}

.tip-icon-check {
  font-size: 14px;
  color: #10b981;
  flex-shrink: 0;
  width: 24px;
  height: 24px;
  background: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 700;
}

.tip-content-show {
  flex: 1;
}

.tip-title-show {
  font-size: 14px;
  font-weight: 600;
  color: #166534;
  margin: 0 0 4px 0;
}

.tip-description-show {
  font-size: 13px;
  color: #15803d;
  margin: 0;
  line-height: 1.5;
}

/* Responsive */
@media (max-width: 1024px) {
  .overview-top-row {
    grid-template-columns: 1fr;
    gap: 20px;
  }

  .overview-bottom-row {
    grid-template-columns: 1fr;
    gap: 20px;
  }
}

@media (max-width: 768px) {
  .nutrition-plan-show-layout {
    flex-direction: column;
  }

  .left-sidebar {
    width: 100%;
    height: auto;
    position: relative;
  }

  .main-content-show {
    padding: 24px 16px;
  }

  .plan-show-header {
    flex-direction: column;
    gap: 16px;
    align-items: stretch;
  }

  .header-actions-show {
    flex-direction: column;
  }

  .btn-header-action,
  .btn-header-share {
    width: 100%;
    text-align: center;
    justify-content: center;
  }
}
</style>
